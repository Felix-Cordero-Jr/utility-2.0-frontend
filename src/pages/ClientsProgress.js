import React from 'react';
import { Typography } from '@mui/material';

const ClientsProgress = () => {
  return (
    <div>
      <Typography variant="h4">Clients' Progress</Typography>
    </div>
  );
};

export default ClientsProgress;
