import React from 'react';
import { Typography } from '@mui/material';

const Archives = () => {
  return (
    <div>
      <Typography variant="h4">Archives Page</Typography>
    </div>
  );
};

export default Archives;
