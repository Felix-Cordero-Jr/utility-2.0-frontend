import React from 'react';
import { Typography } from '@mui/material';

const FormsOverview = () => {
  return (
    <div>
      <Typography variant="h4">Forms Overview</Typography>
    </div>
  );
};

export default FormsOverview;
