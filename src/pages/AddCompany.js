import React from 'react';
import { Typography } from '@mui/material';

const AddCompany = () => {
  return (
    <div>
      <Typography variant="h4">Add a New Company</Typography>
    </div>
  );
};

export default AddCompany;
