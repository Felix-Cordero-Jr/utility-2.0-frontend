import React from 'react';
import { Typography } from '@mui/material';

const Companies = () => {
  return (
    <div>
      <Typography variant="h4">Archived Companies</Typography>
    </div>
  );
};

export default Companies;
